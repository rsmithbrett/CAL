# CAL partition layout - 4MB ESP32-WROOM-32E
#
# FORMAT NOTE: no trailing comments on the data rows below. The generator reads
# the column after Size as the flags field, so a "# ..." there is parsed as a
# flag and the build fails with "CSV flag column contains unknown flag". Sizes
# written with thousands separators make it worse, since those commas are column
# separators. Everything explanatory therefore lives up here.
#
# Asymmetric on purpose. A conventional dual-slot OTA layout gives two equal
# ~1.875MB app partitions, and the product application measured 2,166,784 bytes
# - about 196KB over that line. Splitting the flash unevenly instead removes the
# ceiling entirely: CAL is small and permanent, the application slot is large.
#
# factory holds CAL and is never written by OTA. It is therefore the recovery
# image: a bad application build drops back here rather than bricking the unit,
# which matters because the device cannot be serviced remotely.
#
# Consequence: with a single OTA slot the application cannot update itself (it
# cannot write the partition it is executing from). Updates reboot into CAL,
# which performs the download and hands back. See README.
#
# SIZING, MEASURED RATHER THAN ESTIMATED
# --------------------------------------
# CAL compiled to 1,318,891 bytes on 2026-09-02 and 1,337,360 on 2026-09-15 with
# the boot journal in (esp32 core 3.3.11, LovyanGFX, NetworkClientSecure/mbedTLS,
# LittleFS, WebServer, ArduinoJson). An earlier draft of this table gave factory
# 0x130000 = 1,245,184, which CAL exceeded by roughly 72KB - the split was moved
# to fit the measured figure, not the other way round. That 72KB miss is the
# reason every number here is measured and not estimated.
#
# THE 2026-09-15 REBALANCE, AND WHY IT HAD TO HAPPEN NOW
# ------------------------------------------------------
# factory was 0x160000 = 1,441,792, leaving 104,432 bytes over the measured CAL
# (7.2%). That is roughly one core upgrade of margin: mbedTLS or the esp32 core
# can move a binary by tens of KB with no source change here at all.
#
# Running out of factory is the worst of the available failures, because factory
# holds the RECOVERY image. Losing it means keeping the ability to break the
# application while losing the ability to ship the thing that rescues it.
#
# The space came from ota_0, whose reservation was sized for an application of
# 2,166,784 bytes - a figure that existed to keep the Bluetooth/A2DP stack
# possible. BLUETOOTH IS NO LONGER PLANNED (confirmed 2026-09-15), so that
# reservation was holding 682KB against a requirement that no longer exists. The
# application last measured 1,484,976.
#
# factory 0x1A0000 = 1,703,936  ->  366,576 bytes spare over CAL      (21.5%)
# ota_0   0x200000 = 2,097,152  ->  612,176 bytes spare over the App  (29.2%)
#
# The App keeps the larger absolute cushion deliberately: it is where features
# land, so it is the faster grower. CAL is fixed-purpose - provisioning, OTA
# download, splash, journal - and grows mainly from library bloat.
#
# ONLY ota_0 MOVES. nvs, otadata, callog, spiffs and coredump keep their exact
# offsets, which is the point rather than a coincidence: nvs holds the device
# secret and its remembered networks, so a unit brought onto this table by
# writing the table and CAL alone stays enrolled and stays on its WiFi.
# factory's OFFSET is unchanged too - only its far wall moves - so the CAL image
# already at 0x10000 remains valid, because an app image declares its own length
# and a larger partition still contains it.
#
# WHY THIS CANNOT BE DONE OVER THE AIR, so nobody re-opens the question:
#   1. The pinned core is built CONFIG_SPI_FLASH_DANGEROUS_WRITE_ABORTS=y. The
#      flash driver treats the partition table as off-limits to raw writes and
#      ABORTS - panics - rather than returning an error. Writing 0x8000 from
#      firmware crashes the device instead of working. Changing that means
#      building a custom esp32 core, not flipping a sketch-level flag.
#   2. Even if it were permitted, writing the table is one sector erase followed
#      by one write, and in between the table is blank. With
#      CONFIG_PARTITION_TABLE_MD5=y a partially written table fails its checksum
#      and is rejected outright, so the bootloader finds nothing at all - not
#      even CAL. There is no redundant copy. The window is milliseconds and the
#      outcome is unrecoverable without a cable.
#   3. Over USB the same operation has a safety net that cannot be removed: the
#      first-stage bootloader is in ROM, in silicon. A flash interrupted at any
#      point still leaves a chip that enters download mode, so the fix for a
#      failed attempt is to run it again.
# The cable risks nothing permanent. The radio risks the one outcome that cannot
# be repaired remotely. Re-table units while somebody can still reach them.
#
# Re-measure both figures before trusting this table again. CAL is the partition
# that cannot be resized in the field: every unit already flashed keeps whatever
# table it was given, so growing factory later means recovering hardware.
#
# A unit left on the OLD table is not broken. It has less factory headroom and
# nothing else changes - and since CAL is installed over USB only, a device that
# nobody can physically reach could never take a new CAL regardless of its table.
#
# App partitions must start on a 64KB boundary. 0x10000, 0x1B0000 and the sizes
# below all satisfy that, and the table fills the 4MB device exactly.
#
# CALLOG, ADDED AFTER THE FACT, AND WHY IT COST ALMOST NOTHING
# ------------------------------------------------------------
# callog is CAL's boot journal - 16 raw 4KB sectors, one boot per sector, read
# over USB at a bench. It exists because a device found showing "Update failed"
# left no record of anything: the App owns the remote debug stream, and the App
# is exactly what CAL had failed to reach. See the README's "CAL can say why
# now" for the argument, including why NVS, LittleFS, RTC memory and the
# coredump partition were each rejected before this.
#
# The 64KB came out of ota_0, which held the only real slack here. factory was
# NOT touched - that is this file's oldest warning and the one that cannot be
# walked back - and no existing partition's OFFSET moved. ota_0 only shrank,
# and it is still far larger than anything installed in it:
#
# ota_0 was 0x240000 = 2,359,296 when callog was carved out of it. The 2026-09-15
# rebalance above took a further 262,144 for factory, leaving 0x200000 =
# 2,097,152. callog itself did not move and is unaffected by that change.
#
# Because nothing moved, an already-flashed unit is brought onto this table by
# writing the table alone (esptool write_flash 0x8000 CAL.ino.partitions.bin).
# factory, ota_0, nvs, otadata and spiffs survive byte-for-byte: the device
# keeps its secret, its remembered networks and its installed App.
#
# A unit that is never reflashed is not broken by this either. Journal.cpp
# looks callog up by NAME, finds nothing on an older table, disables itself and
# says so on serial. New CAL on an old table behaves like old CAL did.
#
# coredump is NOT spare space, whatever it looks like: the pinned core builds
# with CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH=y, so panic backtraces are already
# written there. They cover the one case the journal structurally cannot - a
# crash that never reaches a log call - so the two are complements.
#
# Name,     Type, SubType,  Offset,   Size,     Notes
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xE000,   0x2000,
factory,    app,  factory,  0x10000,  0x1A0000,
ota_0,      app,  ota_0,    0x1B0000, 0x200000,
callog,     data, 0x99,     0x3B0000, 0x10000,
spiffs,     data, spiffs,   0x3C0000, 0x30000,
coredump,   data, coredump, 0x3F0000, 0x10000,
